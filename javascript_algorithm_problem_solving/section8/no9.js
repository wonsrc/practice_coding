function solution(coins, charge) {
    let answer = 0;

    function DFS(L, sum) {
        if( === DFS(L)) {

        } else {

        }
    }

    DFS(0,0);
    return answer;
}

let coins = [1, 2, 5];
let charge = 15;

console.log(solution(coins, charge));